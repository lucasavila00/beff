---
source: packages/beff-core/tests/safe_to_change.rs
expression: "print_errors(from, to, &errors)"
---
from:

        export default {
            "/hello": {
                get: ():1 => todo()
            }
        }
        
to:

        export default {
            "/hello": {
                get: () => todo()
            }
        }
        

# Error at GET /hello:

Response body is not compatible.

```ts
type Old = 1;
```

```ts
type New = (null | boolean | string | number | Object | Array);
```

```ts
type Diff = (null | boolean | string | Object | Array | Not<1>);
```

```ts
type Evidence = string;
```

Previous clients will not support this potential response:

evidence:

```json
"abc"
```
